#!/usr/bin/python3
def best_score(a_dictionary):
	new_list = (a_dictionary.key())
	score = 0
	leader = ""
	for i in my_list:
		if a_dictionary[i] > score:
			score = a_dictionary[i]
			leader = i
	return leader
		
